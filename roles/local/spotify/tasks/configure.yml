---
- name: 'Create Spotify configuration directory'
  file:
    path: '{{ spotify_config_root }}'
    recurse: yes
    state: 'directory'
    owner: '{{ configure_user_name }}'
    group: '{{ configure_user_name }}'

- name: 'Copy template config file to Spotify root'
  template:
    src: 'prefs.j2'
    dest: '{{ spotify_config_path }}'
    follow: yes
    group: '{{ configure_user_name }}'
    owner: '{{ configure_user_name }}'
    mode: 0664
