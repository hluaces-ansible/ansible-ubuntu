# hluaces.molecule

Molecule platform definitions for libvirt and Docker. Reusable
create/destroy playbooks and roles for Molecule testing scenarios.

Vendored from `igalia.molecule` (namespace re-stamped to `hluaces`).
